# Allow installing dev deps when peer dependency ranges don't include React 19 yet
# This suppresses strict peer-dependency failures (keeps warnings). Update when libs publish React 19 support.
strict-peer-dependencies=false
